# собрать в фаил информацию с серверов, сколько у них ЦПУ
---
- name: output cpu to file
  hosts: all
  become: yes

  tasks:
  - debug:
     var: ansible_processor_count

  - shell: df -h | grep /boot
    register: results

  - debug:
     var: results.stdout_lines
